@model CleanHub.ViewModels.CardsViewModel

@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<hr />
<form asp-action="BuildingsFinance" asp-controller="Cards" method="get" id="filterForm">
    <div class="row">
        @if (ViewBag.Buildings != null)
        {
            <div class="col-lg-2 col-md-3">
                <div class="form-group mb-0">
                    <label for="buildingSearch">Објект</label>
                    <input class="form-control" list="buildingOptions" id="buildingSearch" name="buildingName" placeholder="Пребарувај">
                    <input type="hidden" id="buildingId" name="buildingId">
                    <datalist id="buildingOptions">
                        @foreach (var building in (IEnumerable<SelectListItem>)ViewBag.Buildings)
                        {
                            <option value="@building.Text" selected="@ViewBag.Selected" data-id="@building.Value"></option>
                        }
                    </datalist>
                </div>
            </div>
        }
        else
        {
            <p>Нема пронајдено објекти!</p>
        }

        <div class="col-lg-2 col-md-3">
            <div class="form-group mb-0">
                <h6>Од</h6>
                <input id="dateFrom" name="dateFrom" placeholder="ден.месец.година" type="text" class="form-control flatpickr flatpickr-input active" value="@ViewBag.DateFrom" />
            </div>
        </div>
        <div class="col-lg-2 col-md-3">
            <h6>До</h6>
            <div class="form-group mb-0">
                <input id="dateTo" name="dateTo" placeholder="ден.месец.година" type="text" class="form-control flatpickr flatpickr-input active" value="@ViewBag.DateTo" />
            </div>
        </div>
    </div>
    <br />
    <div class="col-xl-2 col-lg-6 col-md-6 col-sm-6">
        <div class="form-group mb-0">
            <button type="submit" class="btn btn-primary">Пребарувај</button>
        </div>
    </div>
</form>
<div class="col-xl-12 col-lg-12 col-sm-12 layout-spacing">
    </br>
    <div class="statbox widget box box-shadow">
        <div class="widget-content widget-content-area">
            <table id="html5-extension" class="table dt-table-hover" style="width:100%">
                <thead>
                    <tr role="row">
                        <th data-sorter="false">Станар</th>
                        <th data-sorter="false">Адреса</th>
                        <th data-sorter="false">Побарува</th>
                        <th data-sorter="false">Должи</th>
                        <th data-sorter="false">Салдо</>
                    </tr>
                </thead>
                <tbody>
                    @if (Model.BuildingFinanceCardViewModels != null && Model.BuildingFinanceCardViewModels.Any())
                    {
                        @foreach (var invoice in Model.BuildingFinanceCardViewModels)
                        {
                            <tr role="row">
                                <td>@Html.DisplayFor(modelItem => invoice.CustomerInfo)</td>
                                <td>@Html.DisplayFor(modelItem => invoice.Adress)</td>
                                <td>@Html.DisplayFor(modelItem => invoice.Demands) МКД</td>
                                <td>@Html.DisplayFor(modelItem => invoice.Owes) МКД</td>
                                <td></td>

                            </tr>
                        }
                        <tr role="row">
                            <td><strong>Вкупно</strong></td>
                            <td></td>
                            <td><strong>@Model.CustomerDemandsTotal МКД</strong></td>
                            <td><strong>@Model.CustomerOwesTotal МКД</strong></td>
                            <td>
                                <strong>
                                    Салдо :
                                    @(Model.CustomerOwesTotal > Model.CustomerDemandsTotal ? "- " : "")
                                    @Model.Total МКД
                                </strong>
                            </td>
                        </tr>
                    }
                    else
                    {
                        <tr>
                            <td colspan="5" class="text-center">Нема пронајдени податоци</td>
                        </tr>
                    }
                </tbody>
                <tfoot>
                    <tr>
                        <th>Вкупно</th>
                        <th></th>
                        <th>Побарува: @Model.CustomerDemandsTotal МКД</th>
                        <th>Должи: @Model.CustomerOwesTotal МКД</th>
                        <th>
                            Салдо: @(Model.CustomerOwesTotal > Model.CustomerDemandsTotal ? "- " : "") @Model.Total МКД
                        </th>
                    </tr>
                </tfoot>
            </table>
        </div>
    </div>
</div>

<script>
    document.getElementById('buildingSearch').addEventListener('input', function () {
        const selectedValue = this.value;
        const options = document.querySelectorAll('#buildingOptions option');
        let selectedId = '';

        // Find the matching option and get its data-id
        options.forEach(option => {
            if (option.value === selectedValue) {
                selectedId = option.getAttribute('data-id');
            }
        });

        // Set the hidden input's value
        document.getElementById('buildingId').value = selectedId || '';
    });
</script>